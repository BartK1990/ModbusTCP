<UserControl x:Class="ModbusTCP.View.CommunicationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModbusTCP.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="560">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0 0 0 0">
            <Button Margin="20 0 0 0" Content="Send" x:Name="Send_Button" Command="{Binding SendCommand}" Height="30" HorizontalAlignment="Left" Width="100" FontSize="16" FontWeight="Bold"/>
        </DockPanel>
        <DockPanel Grid.Row="1" Margin="0 0 0 0">
            <ListBox Margin="0 0 0 0" VerticalAlignment="Top"/>
        </DockPanel>
    </Grid>
</UserControl>
